[project]
name = "{{name}}"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.0",
]

[tool.ruff]
src = ["."]
line-length = 88

[tool.pytest.ini_options]
testpaths = ["tests"]
