{
  "name": "{{name}}",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src"
  },
  "dependencies": {
    "@m2s2/vue-lib": "^2.9.3",
    "@m2s2/tokens": "^2.2.2",
    "vue": "^{{vue}}"{{#if auth}},
    "aws-amplify": "^{{aws_amplify}}",
    "@aws-amplify/ui-vue": "^{{_aws_amplify_ui_vue}}"{{/if}}{{#if billing}},
    "@stripe/stripe-js": "^{{_stripe_stripe_js}}"{{/if}}
  },
  "devDependencies": {
    "@eslint/js": "^{{_eslint_js}}",
    "@testing-library/jest-dom": "^{{_testing_library_jest_dom}}",
    "@testing-library/vue": "^{{_testing_library_vue}}",
    "@vitejs/plugin-vue": "^{{_vitejs_plugin_vue}}",
    "eslint": "^{{eslint}}",
    "eslint-plugin-vue": "^{{eslint_plugin_vue}}",
    "jsdom": "^{{jsdom}}",
    "sass-embedded": "^{{sass_embedded}}",
    "typescript": "~{{typescript}}",
    "typescript-eslint": "^{{typescript_eslint}}",
    "vite": "^{{vite}}",
    "vitest": "^{{vitest}}",
    "vue-tsc": "^{{vue_tsc}}"
  }
}
